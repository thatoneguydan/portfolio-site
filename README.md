# dantylersmith.com portfolio

Public deployment repository for Dan Tyler Smith's portfolio.

The site content is published only from the SHA-256-verified public candidate produced by the private `portfolio-backend` migration/build pipeline. Migration captures, internal evidence, private tooling, and source-only metadata are not stored here.

The production domain is not cut over during initial publication; the temporary GitHub Pages site is verified first.
